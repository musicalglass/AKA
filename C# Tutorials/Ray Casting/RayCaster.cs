using System;

class RayCaster
{
   static void Main ( )
   {
   // Initialize all Variables
   float StartAngle ;
   float ViewAngle ;

   //Start the cast -30 degrees from the view direction
StartAngle = ViewAngle - 30 ;

//Cast 320 rays,  one for each screen column
	for ( int ray = 0 ; ray < 320 ; ray++ )
	{
	// compute the slope of the current ray
	

	}

   }
} 